<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 inline-block" href="/logout">Logut</a>
        </div>
        <h1 class="text-3xl mb-2 text-white">Hello, <%= user.name %></h1>
        <h5 class="mb-5">You can create a new post.</h5>
        <form action="/post" method="post">
            <textarea placeholder="What's on you mind..." name="content" class="p-3 w-1/3 bg-transparent border-zinc-800 mb-5 rounded-md resize-none border-2 outline-none"></textarea>
            <input class="px-3 w-40 py-2 text-sm bg-blue-500 block rounded-md" type="submit" value="Create New Post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="post-container flex gap-2 mt-5">
                <% user.posts.forEach(function(post){
                    %>
                    <div class="post w-1/3 mt-2 p-4 rounded-md border-zinc-800 border-2">
                        <h4 class="text-blue-500 mb-2 cursor-pointer">@<%=user.userName %></h4>
                        <p><%= post.content %></p>
                        <small class="mt-2 inline-block"><%= post.likes.length %> likes</small>
                        <div class="btns flex gap-4 mt-1">
                            <a href="/like/<%=post._id %>" class="text-blue-500"><%= post.likes.indexOf(user._id) ===  -1 ? "Like" : "Unlike" %></a>
                            <a href="/edit/<%=post._id %>" class="text-zinc-400">Edit</a>
                        </div>
                    </div>
                    <%
                }) %>
              
            </div>
        </div>
    </div>

</body>

</html>