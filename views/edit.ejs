<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 inline-block" href="/logout">Logut</a>
        </div>
        <h5 class="mb-5">Edit your post.</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea name="content" class="p-3 w-1/3 bg-transparent border-zinc-800 mb-5 rounded-md resize-none border-2 outline-none"><%= post.content %></textarea>
            <input class="px-3 w-40 py-2 text-sm bg-yellow-500 block rounded-md" type="submit" value="Update Post">
        </form>

    </div>

</body>

</html>